import { Channel } from 'chaty-client/models'

/**
 * 'Marker' to send position information for mounting the floating call card
 * TODO: implement
 */
export function VoiceChannelCallCardMount({ channel }: { channel: Channel }) {
  return <div></div>
}
